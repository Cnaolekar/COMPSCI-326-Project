import { DynamicValue, HtmlElement } from "../dom";
import { Permitted, PermittedAttribute, PermittedOrder } from "./element";
export declare class Validator {
    static validatePermitted(node: HtmlElement, rules: Permitted): boolean;
    static validateOccurrences(node: HtmlElement, rules: Permitted, numSiblings: number): boolean;
    static validateOrder(children: HtmlElement[], rules: PermittedOrder, cb: (node: HtmlElement, prev: HtmlElement) => void): boolean;
    static validateAttribute(key: string, value: string | DynamicValue | null, rules: PermittedAttribute): boolean;
    private static validatePermittedRule;
    private static validatePermittedCategory;
}
